SECRET_KEY=CHANGE_THIS_TO_A_LONG_RANDOM_SECRET
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=YOUR_MYSQL_PASSWORD
DB_NAME=voucher_db
UPLOAD_FOLDER=uploads
PORT=5000

# --- SMS / WhatsApp (Twilio) — used for OTP verification & voucher update alerts ---
# Leave blank to disable; the app will log to console instead of sending.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_SMS_FROM=
TWILIO_WHATSAPP_FROM=

# --- Email (SMTP) — used for voucher update alerts ---
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
